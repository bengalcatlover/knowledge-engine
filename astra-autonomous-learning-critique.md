# Fable案の厳密レビューと、90日で実装する確定案

> **レビュー範囲**：提示された設計書を対象とします。参照先の `KNOWLEDGE_ARCHITECTURE.md` は未添付で、§7も途中で切れているため、5層モデルとの整合性と評価計画の全体は確認できません。以下は設計監査であり、講義・論文の実査や実装の動作検証ではありません。

**結論：方向性は採用できますが、「保存規則を守れば知識の正しさも守れる」という混同を修正すべきです。**

採用すべき核は、出典追跡、主張と条件の分離、反証探索、自己引用の禁止、コードによる権限制限です。一方、**JSONL正本、資料数による自動昇格、独立性の自動確定、複数AIの多数決は採用しません。**

最初に作るのは、**資料取得・抽出・追加調査・未解決点の記録までを自動実行し、人間が根拠を確認して採用できる、ローカル中心の調査基盤**です。提案だけで終わらせませんが、収集の自動化と真偽判定の自動化を分けます。

---

## 1. Fable案への評価：採用・修正・延期

### 1.1 採用すべき点

- **MIT講義を概念発見の入口にする**  
  講義の説明構造は探索の足場になります。ただし、講義で発見した概念と、外部資料で裏付けられた主張は別物として扱います。MITという所属自体を真偽の根拠にしません。

- **主張・根拠・成立条件・限界を別々に保存する**  
  単なるノート集やチャンク検索から一歩進む、最も重要な設計です。

- **取得時点の資料と引用位置を保存する**  
  URLだけでは、後から出典の内容を検証できません。

- **反証、限定条件、行き止まりを記録する**  
  成功した調査だけを保存すると、確認バイアスと同じ探索の反復が起きます。

- **AI出力を根拠に昇格させない**  
  要約・回答・グラフの説明文は、探索の補助には使えても独立した証拠ではありません。

- **予算・権限・停止をモデルの善意に任せない**  
  ここは決定論コードの責任です。エージェントフレームワークを最初から導入しない判断も妥当です。

### 1.2 危険な点と修正方針

| 論点 | 問題 | 採用する修正 |
|---|---|---|
| **append-only ledgerを正本にする** | 履歴保全には有効ですが、イベント順序、重複排除、スキーマ移行、再生互換性、削除要請への対応まで背負います。追記専用でも内容の正しさや改ざん耐性は保証されません。 | **SQLiteを正本にし、重要変更の履歴を同一トランザクションで保存**。全面的なイベントソーシングはしない。 |
| **日次JSONL＋Git** | 複数レコードの原子性、途中書込み、同時実行、ledgerとSQLiteの二重書込みが難所です。Gitはデータベースでもバックアップでもありません。 | JSONLは交換・エクスポート用。Gitはコード、スキーマ、設定、評価定義に使用。 |
| **ハッシュがあれば検証可能** | ハッシュで確認できるのは保存後の同一性です。著者の真正性、内容の真実性、取得日時の第三者証明までは保証しません。 | URL、取得日時、文書版、著者、識別子、取得経路も保存。原本と変換後テキストのハッシュを区別。 |
| **引用の実在照合＝検証** | 実在する引用でも、主張を支持していない、否定文を切り取っている、対象条件が違う、といった誤りは残ります。 | 機械照合と、**引用が主張を支えるかの意味的審査**を別工程にする。 |
| **根拠2件・3件で昇格** | 件数は証拠の質ではありません。同じ実験の紹介記事を増やしても知識は強くなりません。 | 自動処理が付けるのは「形式検査済み」等の作業状態。認識上の確かさは件数から決めない。 |
| **反証ゼロなら昇格、1件あれば降格** | 見つからなかったことは反証不存在ではありません。誤抽出した反証1件で既存知識を失効させるのも危険です。 | 反証候補の発見で警告を付け、条件の比較と根拠確認へ回す。採用状態の変更は別判断。 |
| **数値閾値を品質保証に使う** | 類似度0.9、引用失敗率30%、罠検出率90%等に校正根拠がありません。モデル・分野・母数でも意味が変わります。 | 予算・回数などの運用上限と、知識品質の評価値を分離。品質値には母数・誤り分類・不確実性を併記。 |
| **多様性クォータで追加取得を強制** | ドメイン分散は研究の独立性ではありません。一次資料が特定リポジトリに集中する分野で、低品質資料と費用を増やす恐れがあります。 | 偏りの警告として使い、機械的な資料数稼ぎはしない。 |
| **別モデルの検証・多数決** | 見落としの減少には役立ちますが、学習資料や推論傾向が重なるため、独立した検証者とは限りません。 | 第二モデルは反例・条件欠落の発見に限定して活用。票数を確信度にしない。 |
| **再検証失敗でretired** | 見つけ直せないことと、誤っていることは別です。講義概念を無期限とするのも粗すぎます。 | 「再確認期限超過」「出典取得不能」「反証あり」「置換済み」を分離する。 |
| **コード部は失敗しない** | DB破損、権限、API仕様変更、文字コード、費用計測などで失敗します。 | 不明な状態では安全側に停止。再試行可能性と冪等性を設計する。 |
| **robots.txt遵守＋承認で取得可** | robots.txtは利用許諾そのものではなく、人間承認も規約違反を適法化しません。 | 公開性、利用条件、保存・再配布条件を別途確認。不明な取得は保留。 |

#### 独立性は「決定論的に確定できる属性」ではない

Fable案の `independence_group` は、最も大きく修正すべき箇所です。

- 同じ出版社やarXiv上にある論文でも、別の実験・データである場合があります。
- 別ドメインでも、同じ論文・プレスリリース・データセットの言い換えかもしれません。
- 相互引用は、依存だけでなく批判や追試を表すこともあります。
- 共著者が一人重なることと、証拠が全面的に同一であることは違います。

したがって、資料を単一グループに押し込まず、**主張ごとに「同じデータを使用」「原資料の紹介」「独立した追試」「関係不明」などの依存関係を記録**します。

コードはDOI一致や明示的な転載関係を検出できます。AIは依存の疑いを提案できます。しかし、重要な主張の独立性は根拠付きで人間が確認します。**不明を独立扱いせず、不明を同一資料と断定もしません。**

また、必要な裏付けは主張の種類によります。「規格の現行版はこう定める」なら当該規格が中心です。数学的主張なら証明の妥当性が中心です。あらゆる知識に「独立資料3件」を要求するのは不適切です。

### 1.3 今は作らないもの

1. **GraphRAGとグラフ全体の自動要約**  
   最初はSQLite FTS5と明示的な関係表で十分です。GraphRAGは検索方式であり、真実性の保証機構ではありません。導入は、実際の質問群で通常検索より改善することを確認してからです。

2. **独立プロセスのマルチエージェント群**  
   まず単一ワーカーと永続ジョブキューにします。プロセス分割は証拠の独立性を生みません。

3. **全エッジの独立した昇格制度**  
   主張とエッジの二重審査は負担が大きすぎます。因果関係など重要な関係自体を、条件付きの主張として保存します。

4. **Web全体に対する自己転載検出**  
   n-gram一致は補助にはなりますが、言い換え転載は見逃し、一般表現は誤検出します。完全防御として扱いません。初期は非公開運用と出典系譜の記録を優先します。

5. **AIによる計算実験の自由実行、モデルの追加学習、完全自律公開**  
   90日では対象外です。検索・保存による学習と、モデル重みの学習を混同しません。

なお、設計書内にも、`accepted` と `verified` の混在、S6でcandidate間に張る矛盾エッジと§5の制約の衝突、`experiment_internal` の型定義不足があります。**実装前に状態表とスキーマを一本化する必要があります。**

---

## 2. 最終アーキテクチャ

### 2.1 正本と派生物

```text
MIT講義・外部資料
        ↓ 許可された取得処理
原本ファイル保存 ── SQLite：出典・主張・証拠・判断・ジョブ
                              ↑
                     狭い登録API／検証コード
                              ↑
                        AIの構造化提案

SQLite＋原本
   ├─ 全文検索インデックス
   ├─ 概念グラフ表示
   ├─ Markdownノート・レビュー画面
   └─ 出典付き調査レポート
      ※いずれも派生物。根拠への再投入は禁止
```

| 区分 | 確定内容 |
|---|---|
| **構造化データの正本** | SQLite。出典、主張、証拠との対応、判断、調査課題、実行記録を保持。外部キー・制約・トランザクションを使用。 |
| **取得資料の正本** | 内容ハッシュで管理する原本ファイル。取得可能ならPDF・HTML等の元データを保存。抽出テキストは変換版として別管理。 |
| **変更履歴** | 主張・引用対応・判断の改訂履歴をSQLite内に保持。現行状態と履歴を同一トランザクションで更新。全状態のイベント再生は要求しない。 |
| **派生物** | FTSインデックス、必要になった場合の埋め込み、グラフ表示、Markdown、回答、JSONL出力。削除・再生成可能。 |
| **バックアップ** | SQLiteの整合したバックアップと、対応する原本一覧・ファイルを一組で保存。復元試験を実施。Gitで代用しない。 |

**正本は「真実」ではなく、何を取得し、どう解釈し、誰がどの条件で採用したかの正規記録です。**

原本は原則として上書きしませんが、個人情報・権利・保持期限に伴う削除は可能にします。削除理由は残しても、削除対象の本文を監査ログやバックアップに永久残存させない運用が必要です。

### 2.2 最小データモデル

物理テーブルを過度に分割せず、次の単位から始めます。

| 単位 | 必須内容 |
|---|---|
| **source / artifact** | URL・文書識別子、著者、公開日・版、取得日時、原本ハッシュ、抽出方式、利用条件、入手できた範囲 |
| **concept** | 名前、別名、対象範囲、発見元。定義の実質的内容は主張と出典に接続 |
| **claim** | 主張、主張の種類、対象・条件・限界、資料上の時点、作業状態、改訂元 |
| **evidence_link** | 主張ID、出典の版、引用、位置、前後文脈、支持／反証／限定の候補判定、その判定理由 |
| **relation** | 概念と主張の接続、資料の依存関係、条件付きの主張間関係。種類に応じた出所 |
| **question / run** | 問い、発生理由、調査計画、実行クエリ、取得・除外資料、費用、回答・部分回答・行き止まり |
| **proposal / review / audit** | AI草案、人間判断、変更前後、モデル・設定・ツール実行の記録 |

重要な修正点は次のとおりです。

- 条件や限界が資料に書かれていなければ、**「記載なし／未確認」**と保存します。必須欄を埋めるためにAIに創作させません。
- 「1文」「常にという表現を禁止」といった表面的制約ではなく、**独立に評価できる主張単位**を目指します。
- 引用500字の固定上限は設けず、文脈確認に必要な範囲を保存・参照します。ただし、外部表示・再配布には別の制限を設けます。
- `meta:supersedes` の根拠は変更履歴、ユーザー採用の根拠は人間判断です。**すべてのエッジに論文のevidenceを要求する必要はありません。**

### 2.3 状態は「作業」「評価」「鮮度」に分ける

単一の `candidate → supported → verified` は廃止します。

- **作業状態**：草案／形式検査済み／人間確認済み
- **根拠評価**：未評価／限定条件下で支持／対立あり／根拠不足
- **鮮度・利用状態**：再確認不要／要再確認／旧版／置換済み

「人間確認済み」は絶対的な真実を意味しません。確認した範囲・根拠・時点を持ちます。

新しい反証候補が見つかったら、既存の採用記録を消さずに**「反証候補あり・再審査待ち」**を付けます。回答では警告を隠さず表示します。

### 2.4 AI・決定論コード・人間の責任

| 主体 | 担当 | 担当させないこと |
|---|---|---|
| **AI** | 概念候補の抽出、条件整理、問いと検索計画、関連度判定、支持関係の候補評価、反例探索、暫定回答 | 真偽の確定、独立性の無根拠な確定、予算・権限の変更、任意のDB・ファイル操作 |
| **決定論コード** | 取得、保存、引用照合、整合性検査、権限・予算、重複実行防止、状態遷移の手続、表示上の区別 | 引用の意味的含意や科学的妥当性の保証 |
| **人間** | 重要主張の採用、独立性と条件の確認、矛盾解消、範囲・権限の変更、公開判断 | 毎回の検索語作成や全API呼出しの承認 |

**人間承認点を3種類に固定する設計は採用しません。** 初期90日は以下を承認対象にします。

- 回答で確立した前提として使う主張の採用
- 概念統合や条件変更など、既存知識の意味を変える変更
- 新しい取得権限、非公開資料、外部送信・公開・購入
- スキーマ、予算、検証の厳しさ、実行権限を変える変更

通常の公開資料調査は、事前承認した範囲・予算内で自動実行します。

### 2.5 自律学習ループ

1. 利用者の目的に照らして、未回答・条件不足・反証候補・要再確認を列挙する。
2. AIが、その穴を埋める価値と調査方法を提案する。
3. コードが範囲・費用・取得権限を検査する。
4. 実際に検索・取得・原本保存する。
5. 主張・引用・条件を抽出し、形式検査済みの未審査レコードとして保存する。
6. 重要な候補について反証・境界条件を追加調査する。
7. **増えた根拠、変わった理解、残った不確実性を含む調査報告を生成する。**
8. 人間レビューへ回すか、予算内で次の問いへ進む。

孤立ノードや根拠数の少なさは、知識の穴の**候補**にすぎません。グラフを密にすることを目的関数にしません。

また、CodexとClaude Codeは主に**開発・テスト作成の道具**として使用します。本番の調査ワーカーは制限されたモデルAPIとツールを使い、広いシェル権限やDB直接書込み権限を渡しません。モデルに「書くな」と指示するだけでは分離になりません。

---

## 3. 最初の90日で作る最小構成

### 3.1 初期値

以下は品質の証明ではなく、**費用と運用負荷を抑える暫定上限**です。

| 項目 | 初期値 |
|---|---|
| 対象 | リチウムイオン電池の劣化機構の文献理解。寿命保証、安全設計、実機実験は対象外 |
| 入口 | MIT OCWの1講義系列から、対象に関連する2回程度を人間が選定 |
| 開発規模 | Python、SQLite、ローカル原本保存、CLI、静的Markdown。単一ユーザー・単一ワーカー |
| 開発時間 | 週8〜12時間を想定。未経験なら範囲を減らし、全面実装を2週間とは見積もらない |
| 外部API費用 | 月50米ドル、日2米ドル、1調査1米ドルの上限。開発用サブスクリプション費用は別 |
| 自動実行 | 1日1調査、検索は最大2ラウンド、原本取得は最大5件 |
| レビュー滞留 | 未レビュー調査が10件に達したら新規探索を停止 |
| 検索 | SQLite FTS5＋別名辞書。埋め込み・GraphRAGは使わない |
| 取得範囲 | 公開・利用条件を確認できる資料のみ。ログイン、有料取得、外部公開なし |

費用は呼出し後に数えるだけでなく、**呼出し前に入力・最大出力・検索料金を保守的に見積もって予約**します。料金不明や計測不整合では停止します。可能ならプロバイダー側にも利用上限を設定します。

### 3.2 段階別の成果物と完了条件

| 期間 | 具体的な成果物 | 完了条件 |
|---|---|---|
| **1〜14日：保存と追跡** | リポジトリ、スキーマと移行処理、原本保存、引用位置モデル、登録・閲覧CLI、バックアップ・復元、予算ラッパー | 少数の実資料で、表示された引用から保存原本の該当箇所へ到達できる。途中停止・同一処理再実行で重複や不整合が生じない。別環境へ復元できる。 |
| **15〜30日：人間付きの抽出** | 講義からの概念抽出、外部資料登録、主張・条件・限界の抽出、レビューキュー、FTS検索、出典付き回答 | 目安として講義2回分、外部資料10〜15件、主張30〜50件を扱う。件数充足より、採用した主張すべてで出典・条件・確認者が追跡できることを優先。 |
| **31〜60日：有界な自律調査** | 永続ジョブキュー、検索API連携、問い生成、反証探索、行き止まり記録、日次報告、緊急停止 | 人間が検索クエリを毎回書かずに、少なくとも10調査を取得・保存・報告まで完走。予算超過、取得失敗、引用不一致、重複ジョブ、悪意ある文書指示の試験が安全に終了。 |
| **61〜90日：品質評価と運用固定** | 小規模評価セット、誤り台帳、レビュー時間・費用集計、SQLベースの概念グラフ、運用手順書 | 2週間連続運用で費用と処理が追跡可能。採用主張の監査と復元試験を完了。単純な全文検索＋出典付き回答と比較し、改善点と悪化点を説明できる。 |

**90日後の完成品**は、巨大な知識グラフではありません。

- 出典まで戻れる知識ノート
- 条件付きの採用主張と、未審査候補の明確な分離
- 調査目的に関係する概念グラフ
- 自分で追加調査し、取得結果と未解決点を残すワーカー
- 費用・品質・判断履歴を確認できる運用基盤

です。

### 3.3 評価と停止条件

評価セットは、まず人間が根拠を確認できる**30〜50項目**程度に絞ります。開発用と未使用の確認用を分け、本番知識へ罠問題を混ぜません。

評価対象：

- **引用の正確性**：文言だけでなく版・位置が正しいか
- **支持関係**：引用が主張を実際に支えているか
- **条件の保持**：対象、時点、方法、限定条件が落ちていないか
- **依存の見抜き**：転載・同一データ由来を独立な裏付けと扱わないか
- **不確実性の表現**：未取得・未発見を、不存在や反証と取り違えないか
- **利用価値**：同程度の費用で、人手確認時間や未解決点の把握が改善するか

重大な誤帰属、無断アクセス、未審査主張を確立事項として提示する不具合が見つかれば、該当する自動処理を止めて修正します。

ただし、**「この評価で重大誤りが0件」と「重大誤り率が0%」は違います。** 件数と誤り例を報告し、小さな評価セットの合格を完全自律化の根拠にしません。

---

## 4. ユーザーが今決めるべき前提：最大5問

未回答でも、以下の初期値で保存基盤とテストの実装は開始できます。

1. **最初の対象分野と、その知識を使って行いたい判断は何ですか？**  
   初期値：電池劣化機構の文献理解・学習支援のみ。製品の安全判断や寿命保証には使わない。

2. **週に確保できる開発時間と、根拠レビュー時間はどの程度ですか？**  
   初期値：開発8時間、レビュー1時間。分野の専門家でなければ「原文との整合確認」と「科学的妥当性の確認」を区別して記録する。

3. **開発用ツールを除く、モデルAPI・検索APIの月額上限はいくらですか？**  
   初期値：月50米ドル。Codex／Claude Codeの契約で本番API利用が賄えるとは仮定しない。

4. **扱う資料は公開資料だけですか、それとも私有PDF・社内資料・個人情報も含みますか？**  
   初期値：公開資料のみ。保存はローカル、必要な抜粋だけを外部モデルへ送信。送信条件が確認できない資料は保留する。

5. **どこまで自動実行を許し、最初の出力を何にしますか？**  
   初期値：範囲内の公開資料調査・未審査レコード保存・内部レポート作成まで自動。採用判断と外部公開は人間承認。出力は日次調査報告と週次の知識ノート。

---

## 5. 科学的に慎重な探索にするための非交渉条件

1. **保存されたこと、形式検査を通ったこと、科学的に支持されたことを混同しない。**  
   コードが保証するのは手続と整合性であり、内容の真実性ではありません。

2. **採用主張から、原資料の版・引用位置・条件・判断履歴へ遡れる。**  
   出典を取得できない場合は、その制約を明示します。検索スニペットだけで本文を読んだことにしません。

3. **不明を埋めない。**  
   条件、著者、独立性、限界、ライセンスの不明は明示的に保持します。

4. **AIの要約・回答・グラフ要約は、証拠ではない。**  
   検索の手掛かりとして使う場合も、回答の根拠は原資料へ戻します。外部に転載された自己出力も、新しい裏付けにはなりません。

5. **出典の数、ドメインの数、AIの票数を、独立した証拠の数と同一視しない。**  
   根拠の独立性と質は、主張・データ・方法・資料系譜に即して評価します。

6. **引用の一致と、主張への支持を別々に検査する。**  
   否定、限定、比較対象、観測と因果の取り違えを確認します。別モデルの賛同で代用しません。

7. **反証と境界条件を探し、探索の失敗を保存する。**  
   「反証を発見しなかった」は「正しい」ではなく、「行き止まり」は「答えが存在しない」ではありません。新資料や方法変更があれば再探索を許します。

8. **概念グラフを自動的な真理推論器にしない。**  
   類推は問いの種です。因果・含意の連鎖は、同じ名前空間内でも自動的に正当化されません。概念統合には用語の同名異義や条件差の確認が必要です。

9. **外部文書は、命令ではなく未信頼データとして扱う。**  
   文書内の指示でツール権限を変えません。取得処理にはリダイレクト・内部ネットワーク宛てアクセス等の制限を設け、任意コード実行を許しません。

10. **費用・探索回数・レビュー滞留・権限を、実行経路で強制する。**  
    AIが迂回可能な「お願いベース」の上限は上限ではありません。人間未応答時に権限を拡大しません。

11. **評価問題への適応と、知識の改善を区別する。**  
    評価セットを知識正本に混入させず、未使用の確認問題と実運用サンプルを残します。固定された問いの配分で確認バイアスが解決したとは考えません。

12. **監査可能性と完全再現性を区別する。**  
    ハッシュだけでは呼出し内容を復元できません。許される範囲で入力、出力、ツール応答、設定、モデル識別情報を保存します。それでも外部モデルの同一出力再現は保証しません。

13. **内部計算を導入しても、AI作文と観測結果を区別する。**  
    将来の計算実験にはコード・データ・実行環境・検証記録が必要です。自分の実験を「独立資料1件」と機械的に加算しません。初期90日は実装しません。

14. **学習成果をノード数で測らない。**  
    成果は、利用目的に関係する問いについて、追跡可能な根拠が増え、条件と限界が明確になり、誤った採用を減らせたことです。

---

**採用するのは「勝手に確信を強めるAI」ではなく、「許可された範囲で実際に調べ、証拠と不確実性を蓄積し、その更新を監査できる外部知能」です。** この定義なら、完全自律化を待たずに有用な自律学習を始められ、個人開発の費用と運用負荷にも収められます。