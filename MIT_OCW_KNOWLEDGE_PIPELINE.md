# MIT OCW知識パイプライン

## 目的

MIT OCWの講義を、AIの巨大な記憶として投入するのではなく、ユーザーの問い・開発経験・検証済み資料と組み合わせて使える、出典付きの英語知識基盤にする。

## 正本と派生物

```text
公式動画・字幕
  -> raw/                         原資料とメタデータ
  -> 動画別の英語TXT               読み取り可能な原字幕
  -> *.gemini.md / *.sonnet.md / *.terra.md
                                  英語の整形Markdown
  -> MIT検索索引                  再生成可能な派生物
  -> concepts/                    ユーザーが検討する抽象化
```

原資料と整形Markdownは保存する。SQLite索引は削除・再生成できる派生物であり、知識の正本ではない。

## 整形Markdownの内容

各講義の整形Markdownには、元の`TITLE`、`SOURCE`、`URL`、`VIDEO_ID`を残し、次の順で英語のみを保存する。

1. `Key points`
2. `Lecture structure`
3. `Cleaned transcript`

モデルはClaude Haiku 4.5、GPT-5.6 Luna、Gemini 3.1 Flash-Liteへ均等分散する。Fable 5、GPT-6 Astra、および上位モデルは、全件整形には使わず、設計・評価・例外処理のレビューに使う。

## 日本語の扱い

全講義の日本語要約は作らない。英語Markdownを検索し、ユーザーが日本語で質問・記事作成・概念整理を求めたときに、関連する少数の講義断片だけを日本語で説明する。

日本語の概念ノートを保存する必要が生じたときは、講義の翻訳ではなく、`concepts/`または`perspectives/`に「どの考え方を、どんな条件で使うか」として記録する。

## 取得と出力の境界

- 取得時は英語字幕だけを対象とする。日本語字幕の取得はしない。
- 整形モデルは講義外の情報を加えない。
- 検索結果は講義ID、タイトル、元URL、断片を持つ。
- 数値、直接引用、重要な定義は、整形版だけで確定せず原資料を確認する。
- 商用の成果物では、MIT講義を思考の入力として使い、講義の表現・翻訳・要約を直接の素材として再配布しない。

## 索引更新

通常の回答では索引を読むだけにする。講義の整形Markdownが追加された後、保守作業として次を実行する。

```powershell
cd knowledge-engine
python .\mitocw_rag.py build
```
