```yaml
recommended_next:
  step_1:
    task: "b) 2本目の縦串を別ドメインで通す（情報理論を推奨。MIT OCW 4,258本の素材が既にあり、K-0007のような原典確保問題も検証しやすい）。同時にK-0007のFisher 1935 DOI確保を小タスクとして併走させる"
    rationale: >
      現時点のスキーマ・policy engine・契約テストは統計学ドメインn=1でしか検証されていない。
      33枚の移行（a）を先にやると、第二ドメインで発覚するスキーマ欠陥（例: 定理型ドメイン特有の
      derivationエッジ、HYP以外の暫定ノード種別の必要性）が移行済み25枚に波及し手戻り最大化する。
      縦串の手順・契約テスト・stale伝播はそのまま再利用できるため、MVP知見の活用効率が最も高く、
      コストは初回縦串より大幅に低い。ドメイン非依存性の証明自体がデモ価値を持つ。
    deliverable: >
      第二ドメインの縦串（10ノード前後/エッジ/根拠）、契約テスト7本全通過、
      スキーマ差分レポート（変更不要なら「n=2で一般性確認」の記録、
      変更ありなら移行前の修正）、K-0007のblocked解除

  step_2:
    task: "f) search_engine.pyを新スキーマ（node_id/claim@revision）に接続する"
    rationale: >
      2縦串完了時点で「自然言語の質問 → claim到達 → 根拠鎖の提示」という
      ユーザー最終意図（根拠追跡・回答能力）の最小デモが成立する。
      Recall@6=100%の検索資産をそのまま流用でき実装距離が短い。
      さらに共活性化ログのIDが新スキーマに統一されるため、
      Phase 4（c）の前提データ品質が確保される副次効果がある。
      グラフ走査の全面書き換え（d）は不要 — 根拠到達性はmvp_storeの
      policy engineで既に契約テスト通過済みのため、それを呼ぶだけで足りる。
    deliverable: >
      search結果→node_id解決層、「質問→approved claim→evidence locator」の
      E2Eデモスクリプト、新スキーマIDでの共活性化ログ記録

  step_3:
    task: "a) 既存33カードのうち直接変換可能な25枚のみ移行（分割必要な11枚は保留）"
    rationale: >
      step_1でスキーマ一般性が検証され、step_2で移行即座に検索可能＝価値化される状態が
      整った後なら、手戻りリスクが最小。分割必要な11枚を含めないことで
      作業単位を機械的変換に限定でき、マイグレーションスクリプト自体が
      再利用資産になる。旧101エッジのうち移行対象カード間のものだけ
      relation語彙の再判定付きで移す（related 67本の無差別移行はしない）。
    deliverable: >
      25カードの5層スキーマ移行 + 精査済みエッジ、全ノードで契約テスト通過、
      content_hash実値の一括確定（pending_verification解消をここに合流）

  deferred:
    - "c) 共活性化マイニング — ログ蓄積量が不足。step_2でID統一後に蓄積を続け、閾値到達後に着手"
    - "d) graph.py新スキーマ対応 — 根拠到達性はpolicy engineで代替済み。Facet解決・比喩遮断が新スキーマで必要になった時点で移植"
    - "e) policy_engine独立化 — 利用者がmvp_store 1つの間はYAGNI。第三の呼び出し元が出た時点で切り出し"
    - "g) UIプロトタイプ — step_2のE2Eデモが実データの形を確定させてから設計した方が仕様が安定する"
    - "分割必要な11枚の移行 — 分割判断は知的作業でありスクリプト化不能。25枚の運用実績を見てから"
    - "Phase 5/6（TruthValue, CompositionCard）— 2縦串+移行済みコーパスがないと検証対象が足りない"

  rationale_for_ordering: >
    順序の原理は「検証 → 接続 → 展開」。
    最大の手戻りリスクは「n=1のドメインで確定したスキーマへの過適合」であり、
    これを潰すbを最初に置く（判定基準3）。bはMVPの手順・テスト資産を
    そのまま再利用するため知見活用効率も最大（判定基準1）。
    fをdより先に置くのは、デモ可能な成果（根拠鎖付き回答）への距離が
    最短だから（判定基準2）— dは走査エンジンの書き換えでデモに直結しない。
    aを最後に置くのは、移行は「スキーマ確定後・検索接続後」に実行して初めて
    低リスクかつ即価値化されるため。この3ステップ完了時点で
    「2ドメイン + 25カードが検索可能で根拠に戻れる」状態になり、
    UI設計（g）とPhase 4（c）の判断材料が自然に揃う。
```