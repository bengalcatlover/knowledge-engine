知識ベースの設計判断について、実装可能な提言をください。

現状:
- MIT OCW検索断片を根拠に、再利用可能な概念を `concepts/_inbox/K-XXXX.md` にAI草案として置く。
- 36枚の draft カード、107関係。ユーザー承認まで確定知識にはしない。
- 現実・事業・他分野から質問を自己生成し、検索、重複判定、既存カードへの根拠レビュー、新規草案作成へ進む。
- コストは低く保つ。曖昧な新規作成・重複・関係型はFable 5 と GPT-6 Astraの二者レビューにする。

ユーザーの問い:
「結局、何をどれだけ作ればいいのか。オブジェクト指向プログラミング言語のクラス群を参照したらどうか」

次を答えてください。
1. OOPのクラス設計を知識カード群の参照モデルにする妥当性と、危険な比喩を分けて評価する。
2. 単なるカード枚数ではなく、作成停止・拡張・統合を判断する具体的なメトリクスと閾値を提案する。
3. この知識ベースの最小実行可能なアーキテクチャを、概念クラス、関係、実例、根拠、問い合わせログの役割で示す。
4. 現在36カードからの、低コストで段階的な次の目標（カード数の目安も含む）を提案する。

制約: 根拠のない数値の断定は避け、設計上の目安として明示する。カードを独断で承認済みにしない。回答は日本語で簡潔に。
