# MIT OCW知識を使った記事作成

MIT OCWの整形済みTXTは、AIの恒久的な指示ではなく、出典付きの検索対象として使う。

## 作成手順

1. まず索引を更新する。

   ```powershell
   python .\mitocw_rag.py build
   ```

2. 記事テーマを英語の主要語で検索する。

   ```powershell
   python .\mitocw_rag.py search "game theory auction mechanism" --limit 8
   ```

3. 出力された断片を、AIへ次の指示と一緒に渡す。

   ```text
   以下はMIT OpenCourseWare講義から検索した出典付きの断片です。
   これだけを根拠に、日本語のブログ記事案を書いてください。
   根拠がある説明には該当する講義タイトルとURLを添え、講義にない事実・数値・因果関係は補わないでください。
   断定できない点は「講義では扱われていない」と明記してください。
   ```

## 使い分け

- 講義の概念説明、考え方、導入記事: このMIT OCWコーパスを使う。
- 研究結果、統計、医療・政策・現在の出来事についての主張: `accepted/` の検証済みノートか、別途確認した一次資料を使う。
- `skills.md`: この検索手順と出典確認のルールを数行でAIへ教えるためのもの。1,000本以上の講義本文を入れる場所ではない。
