# 長期運用の知識アーキテクチャ

## 結論

MIT講義Markdownをテーマ別フォルダへ手作業で移したり、全件をさらに日本語要約したりしない。4,258本の講義Markdownは、出典付きの探索層として保存し続ける。知識の価値は資料の量ではなく、依頼ごとに少数の根拠を選び、ユーザー自身の判断へ接続し、その結果を役割付きで残せることにある。

## 5つの層

```text
MIT講義Markdown・開発履歴・一次資料
             ↓ 検索
探索層: MIT / 開発履歴 / candidates
             ↓ 抽象化
概念層: concepts
             ↓ ユーザー確認
判断層: perspectives
             ↓ 検証
根拠層: accepted
             ↓ 案件ごとの統合
出力層: outputs
```

| 層 | 保存するもの | AIがしてよいこと | してはいけないこと |
| --- | --- | --- | --- |
| 探索層 | 講義Markdown、開発履歴、未検証候補 | 関連断片を検索し、出典を示す | 全文投入、現在の事実の断定 |
| 概念層 | 複数資料から抽象化した転用可能な考え方 | `concepts/_inbox/`に草案を作る | 草案をユーザーの意見・確定事実にする |
| 判断層 | ユーザーが確認した問い、経験、方針 | 回答・商品設計の前提として使う | コードや講義から動機を捏造する |
| 根拠層 | 主張単位の検証記録 | 事実の根拠として引用する | ノート全体を無条件に真実扱いする |
| 出力層 | 記事、提案、商品設計の下書き | 根拠と判断を統合する | 出力自体を次回の根拠にする |

## MIT講義Markdownの扱い

- 講義Markdownは移動・分割しない。ファイル名、タイトル、元URL、動画IDを保ったまま検索対象にする。
- 検索索引は派生物であり、Markdownが追加・修正された保守時だけ再構築する。
- 回答で読むのは初回6断片、通常は最大12断片・約6,000トークンまでにする。
- 数値、直接引用、重要な定義はAI整形版だけで確定せず、元資料または一次資料で確認する。
- 全件の日本語翻訳・要約は作らない。日本語は、質問や記事作成の時点で必要な断片だけ生成する。

## ノートが増える条件

新しいノートは「資料を読んだ」だけでは作らない。次のいずれかが起きたときに作る。

1. 同じ考え方が複数の講義または開発経験に現れ、再利用価値がある。
2. 顧客案件・記事・実装で、繰り返し使う判断が生まれた。
3. 現在性を持つ重要な主張を、一次資料で確認できた。
4. ユーザーが自分の見立てとして採用または修正した。

これにより、知識基盤は講義の二重保管庫ではなく、仕事を重ねるほど判断の質が上がる記憶になる。

## 1件のノートに必要な最小情報

| 種別 | 必須情報 |
| --- | --- |
| `accepted` | 主張、出典URL・該当箇所、検証日、検証者、適用範囲、限界 |
| `concept` | 出典、抽象化、使える条件、反例、次の検証、作成者・状態 |
| `perspective` | ユーザーの発言または確認記録、日付、適用範囲、留保 |
| `output` | 読者、結論、主張と根拠の対応、使ったconcept/perspective、未確認点 |

## 日常の使い方

ユーザーは日本語で普通に依頼する。CodexとClaude Codeは、必要な役割のノートを先に検索し、必要に応じてMIT講義と開発履歴を追加検索する。回答では、根拠、ユーザーの判断、AIの提案、限界を分ける。

記事や提案を作った後、再利用できる考えが見つかれば`concepts/_inbox/`へ草案を置く。ユーザーが確認したものだけを判断層へ昇格する。
