# 共通の外部知能

このファイルはCodexとClaude Codeが共通して使う頭の設計である。目的は、MIT OCWから得た考え方、検証済みの資料、ユーザー自身の問いと判断を結び、根拠ある記事・提案・回答を作ることにある。

MIT OCWの取得・整形・索引の正本は`MIT_OCW_KNOWLEDGE_PIPELINE.md`に置く。このファイルはその知識を使うときの行動規則だけを定める。

長期的な整理方法とノートを増やす条件は`KNOWLEDGE_ARCHITECTURE.md`に置く。これは構造の説明であり、このファイルの行動規則を変更しない。

## 知識を役割で分ける

| 場所 | 内容 | 出力での扱い |
| --- | --- | --- |
| `mitocw-txt` | MIT OCWの字幕とAI整形版 | 学習・探索の原資料。講義の要約を商用出力へ直接転載しない。 |
| `knowledge-engine/accepted/` | 一次資料で検証済みの知識 | 事実の根拠として使う。 |
| `knowledge-engine/candidates/` | 未検証の調査候補 | 仮説としてのみ使う。 |
| `knowledge-engine/concepts/` | 複数の資料・開発経験から抽象化した再利用可能な考え方 | 出典、適用条件、反例を読んで使う。 |
| `knowledge-engine/perspectives/` | ユーザーの問い、開発経験、仮説、判断 | ユーザーの解釈として使う。外部の事実とは混同しない。 |
| `knowledge-engine/outputs/` | 記事・提案・回答の下書き | 公開物・提出物の作業場所。 |

## MIT OCWの位置付け

- 講義から概念、方法、問題の見方を学び、ユーザー自身の設計や判断に抽象化して使う。
- 講義に由来する説明を使うときは、タイトルと元動画URLを記録する。
- 講義外の数値、研究結果、現在の事実は`accepted/`または一次資料で確認する。

## 依頼を処理する手順

1. 依頼から、目的・読者・判断したいことを特定する。
2. `perspectives/`と確認済みの`concepts/`の検索は必ず実行する。ヒットした概念は回答の科学的根拠として織り込む。なければ、ユーザーにない意見を勝手に作らない。AI草案である`concepts/_inbox/`は検索・引用しない。

   ```powershell
   python .\brain_rag.py search "keyword one keyword two" --limit 6
   ```
3. 過去の開発判断が役立つ場合は、`knowledge-engine/`をカレントディレクトリとして開発索引を検索する。索引更新は、資料を追加・変更した直後だけ行う。

   ```powershell
   python .\development_rag.py build
   python .\development_rag.py search "keyword one keyword two" --limit 6
   ```

4. MIT OCW検索は必ず実行する。英語の主要語2〜5語で検索する。ヒットした科学的根拠は回答に織り込む。ヒット0件の場合のみ省略してよい。索引更新は、講義の追加・整形直後だけ行う。

   ```powershell
   python .\mitocw_rag.py build
   python .\mitocw_rag.py search "keyword one keyword two" --limit 6
   ```

5. 初回に読む本文は全資料合計6断片までにする。追加検索は通常1回まで、合計12断片・約6,000トークン以内に収める。
6. 検索結果から、講義の説明や過去の実装をそのまま繰り返すのではなく、今回の問いに使える概念と判断を抽出する。
7. 出力では「根拠」「ユーザーの解釈」「提案」「限界」を区別する。`outputs/`の下書きを次回の根拠として使わない。

## 検索結果の使用規則

- `mitocw-txt/`配下のファイルを直接開かない。使えるのは検索ヒットの断片だけで、依頼あたり最大6件にする。
- 講義タイトルとURLは検索結果のメタデータからのみ転記する。なければ「URL未確認」と書き、URLを生成しない。
- 検索が弱いときは、専門語・講義名・同義語で最大3回まで再検索する。

## 記事・提案を書く規則

1. 読者の問いを先に置く。
2. 結論を明示する。
3. 根拠を示す。MIT講義を参照した場合は元URLを残す。
4. ユーザー自身の経験・仮説・判断を、根拠とは別に展開する。
5. 反論、適用できない条件、未確認点を示す。

## 概念ノートを増やすとき

MIT講義や開発履歴から使える考え方を見つけたら、AIの草案は`concepts/_inbox/`に置く。ユーザーが確認するまで、草案をユーザー自身の判断として出力に使わない。概念ノートは要約ではない。複数の入力から抽象化された「何を、どんな条件で、どう使うか」を残す。

```md
---
type: concept
topics: []
status: draft
author: ai-draft
sources: []
---

# 概念名

## 原理
## 適用できる条件
## 使えそうな場面
## ユーザー自身の仮説・判断
## 反例・限界
## 次に検証すること
```

`draft`を単独のAIが確定知識へ昇格させない。ユーザーが Fable 5 と GPT-6 Astra に最終判断を委任した場合は、両者が一致して選んだ `approved` / `revise` / `defer` をカード状態へ反映する。不一致は `defer` にする。ユーザーはいつでもこの判断を上書きできる。採用した考えは`perspectives/`へつなぎ、実際の出力で使う。

## 曖昧な判断の二者レビュー

カードの新規作成、既存カードとの重複判定、根拠の射程、または重要な関係の型に迷いがある場合は、`ask_llm.py` を通じて Fable 5 と GPT-6 Astra の両方に同じ判断材料を渡す。両者が一致した `approved` / `revise` / `defer` は、ユーザーが委任した最終判断としてカード状態へ反映する。判断が割れた場合、またはいずれかが根拠不足とした場合は `defer` にして、必要な追加根拠を記録する。

このレビューは曖昧な候補だけに行う。機械的なID採番、既存カードへの明確な文字列一致、MIT検索、証拠台帳の更新には使わず、APIコストを抑える。

## 回答後の概念カード自動増殖

質問への回答を完了した後、以下の手順で概念カードを自動生成する。

### 判定条件
回答の中核を担った概念が、既存カード（`concepts/` および `concepts/_inbox/`）の `canonical_name` にも `aliases` にも含まれていない場合、候補とする。

### 判定しない場合
- 回答がMIT OCW検索を使わなかった場合（一般知識だけで回答した場合）
- 回答が単純な事実確認や手順案内だった場合（抽象化する概念がない場合）

### 自動生成の手順
1. 回答で使った概念のうち、未カード化のものを列挙する（最大3件）
2. `_meta/counters.yaml` の `concept_counter` を読み、次のIDを決定する
3. 該当するMIT OCW検索結果から Source Refs を構成する（検索ヒットの video_id と引用のみ使用。捏造しない）
4. `concepts/_inbox/K-XXXX.md` を生成する（フォーマットは既存カードに準拠）
5. `_meta/counters.yaml` を更新する
6. 生成したカードのリストをユーザーに報告する（「_inbox に N 枚追加した」程度で十分）

### 制約
- 1回の回答につき最大3枚まで
- 既存カードと alias が重複しないこと（重複する場合は生成しない）
- Source Refs は検索結果から確認できたもののみ記載する（ハレーションゼロ方針）
- `status: "draft"`, `authorship: "ai_draft"` で生成する。確定状態への移行は、上記の二者レビューでユーザーが委任した場合に限り、両モデルの一致判定を反映する。
